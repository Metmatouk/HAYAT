using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System;

public class SmoothRotator : MonoBehaviour
{
    /*public int xAxis;
    public int yAxis;
    public int zAxis;

    [SerializeField]
    Transform targetObject;
    [SerializeField]
    private float limit;
    [SerializeField]
    private float addCount;
    [SerializeField]
    FootAngleController foot;

    private float timer;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        timer += Time.deltaTime;
        if(timer > 0.04) {
            if(Math.Abs(targetObject.localEulerAngles.x - foot.x) > limit) {
                targetObject.localEulerAngles = new Vector3(targetObject.localEulerAngles.x + addCount, targetObject.localEulerAngles.y, targetObject.localEulerAngles.z);
            }
            if(Math.Abs(targetObject.localEulerAngles.y - foot.y) > limit) {
                targetObject.localEulerAngles = new Vector3(targetObject.localEulerAngles.x, targetObject.localEulerAngles.y + addCount, targetObject.localEulerAngles.z);
            }
            if(Math.Abs(targetObject.localEulerAngles.z - foot.z) > limit) {
                targetObject.localEulerAngles = new Vector3(targetObject.localEulerAngles.x, targetObject.localEulerAngles.y, targetObject.localEulerAngles.z + addCount);
            }
            timer = 0.0f;
        }
    }*/
}
